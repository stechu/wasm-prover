[package]
name = "wasm-prover"
version = "0.1.0"
edition = "2018"
licence = "GPL 3.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
